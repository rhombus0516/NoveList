<table>
    <thead>
        <%= image_tag @book.get_image(100,100) %>
        <%= @book.title %>
    </thead>
    <tbody>
        <tr>
            <td>
                <%= @book.body %>
            </td>
        </tr>
        <tr><!-- 編集ボタン -->
             <% if @book.user == current_user %>
                    <td><%= link_to "編集する", edit_book_path(@book.id) %></td>
            <% end %>
        </tr>
    </tbody>
</table>    