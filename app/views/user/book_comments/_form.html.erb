<%= form_with model: [book, book_comment], local:false do |f| %>
  <%= f.text_area :comment, rows:'5', placeholder: "コメント", class: "w-100 vh-10", id: "comment_textarea" %>
  <%= f.submit "送信", class: "btn-sm btn-primary" %>
<% end %>